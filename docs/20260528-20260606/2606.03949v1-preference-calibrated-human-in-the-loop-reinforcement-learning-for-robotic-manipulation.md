---
title: Preference-Calibrated Human-in-the-Loop Reinforcement Learning for Robotic Manipulation
title_zh: 基于偏好校准的环内人类强化学习在机器人操作中的应用
authors: "Zeyi Liu, Guangyao Liu, Yinuo Qu, Yuquan Xue, Bofang Jia, Chunhua Yang, Weihua Gui, Keke Huang, Ziwei Wang"
date: 2026-06-02
pdf: "https://arxiv.org/pdf/2606.03949v1"
tags: ["query:rl-path-opt"]
score: 8.0
evidence: 基于偏好校准的机器人操作强化学习，直接用于末端执行器路径优化
tldr: "人类在环强化学习（HIL-RL）通过在线干预提升机器人操作样本效率，但现有方法对所有转移一致分配奖励，导致次优动作被高估。本文提出PACT框架，利用干预隐含偏好信号进行信用重分配：用progress模型识别次优段，构建偏好对定义反事实优势修正Bellman目标，并直接对齐策略与纠正动作。在五个真实机器人操作任务中，成功率平均提升24.5%，收敛速度加快1.3倍，显著改进样本效率与性能。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.03949v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1345, \"height\": 396, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03949v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1443, \"height\": 494, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03949v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1346, \"height\": 849, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03949v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1356, \"height\": 581, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03949v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1407, \"height\": 415, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.03949v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1306, \"height\": 240, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03949v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1018, \"height\": 193, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03949v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 610, \"height\": 240, \"label\": \"Table\"}]"
motivation: 现有HIL-RL方法对成功轨迹中的次优段一致分配信用，导致Q值高估和策略向次优行为偏移。
method: PACT框架通过progress模型识别次优段，利用干预偏好对构建反事实优势来校准Bellman目标，并在有界均值空间直接对齐策略与纠正动作。
result: "在五个真实机器人操作任务上，PACT平均成功率提升24.5%，收敛速度快1.3倍。"
conclusion: PACT通过偏好校准的信用重分配和策略对齐，有效克服了HIL-RL中次优动作的负面影响，提升了样本效率与任务性能。
---

## 摘要
环内人类强化学习（HIL-RL）通过在线人类干预提高了真实机器人操作中的样本效率。然而，成功轨迹可能包含偏离期望任务执行路径并迫使人类干预的次优动作。现有的HIL-RL方法通常对所有转换应用一致的信用分配原则，通过次优段均匀传播折扣终端奖励，忽略了每个转换对任务成功的实际贡献。这过高估计了用于评论家学习的Q值，并间接误导演员更新朝向次优行为模式。为此，我们提出了PACT，一种基于偏好校准的演员-评论家训练框架，利用干预隐含的偏好信号对识别的次优段进行信用重分配，同时直接指导策略训练以实现无偏的评论家-演员学习。具体来说，我们首先设计了一个从人类演示中学习并识别次优段进行信用校正的进度模型。然后，从干预状态下的人类动作和重采样策略动作中，我们构建偏好对以定义一个反事实优势，该优势惩罚所识别次优段的贝尔曼目标，从而实现方向性信用校准。此外，我们直接在受限均值空间中将策略与人类纠正动作对齐，提供除评论家引导更新之外的额外信号。在五个真实机器人操作任务中，PACT平均成功率提高了24.5%，收敛速度加快了1.3倍，从而提升了RL样本效率和性能。代码可在https://anonymous.4open.science/r/HILRL-A1X-BC05获取。

## Abstract
Human-in-the-loop reinforcement learning (HIL-RL) improves sample efficiency in real-robot manipulation through online human intervention. However, successful trajectories may include suboptimal actions that deviate from the desired task-execution path and force human intervention. Existing HIL-RL methods typically apply the consistent credit assignment principle to all transitions, uniformly propagating discounted terminal rewards through suboptimal segments, ignoring the actual contribution of each transition to task success. This overestimates Q-values for critic learning and indirectly misguides actor updates toward suboptimal behavior patterns. To this end, we propose PACT, a Preference-calibrated Actor-Critic Training framework that leverages the implicit preference signals induced by intervention to perform credit reassignment on identified suboptimal segments while directly guiding policy training for unbiased critic-actor learning. Specifically, we first design a progress model that learns from human demonstration and identifies suboptimal segments for credit correction. Then, from the human action and resampled policy action at the intervention state, we build preference pairs to define a counterfactual advantage that penalizes Bellman targets of the identified suboptimal segment, enabling directional credit calibration. Moreover, we directly align the policy with human corrective actions in the bounded mean space, providing an additional signal beyond critic-guided updates. Across five real-robot manipulation tasks, PACT improves the average success rate by 24.5% and achieves 1.3 times faster convergence, thereby improving both RL sample efficiency and performance. Code is available at https://anonymous.4open.science/r/HILRL-A1X-BC05.

---

## 论文详细总结（自动生成）

# 论文结构化总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题背景**：在真实机器人操作中，人类在环强化学习（HIL-RL）通过操作员实时干预提高样本效率。然而，成功轨迹中常包含偏离任务路径的次优动作段，这些段导致了人类干预。
- **现有缺陷**：现有HIL-RL方法（如HIL-SERL）对所有转移采用一致的信用分配原则，将终端成功奖励均匀地反向传播至次优段，导致Q值被高估；进而误导策略（actor）更新向次优行为偏移，降低收敛速度。
- **本文动机**：需要一种能区分轨迹中各段贡献的方法，对次优段进行信用校正，同时利用干预信号直接指导策略学习。

## 2. 提出的方法论：核心思想、关键技术细节

- **核心思想**：将含干预的成功轨迹视为异构数据，通过**进度模型**定位次优段，利用干预状态处的偏好对（人类纠正动作 > 策略动作）进行**反事实优势**计算，修正该段的Bellman目标；同时，在受限均值空间中对齐策略与人类纠正动作，提供额外监督。
- **关键技术细节**：
    1. **进度模型（Progress Model）**：自监督学习，从演示数据中训练一个轻量级进度估计器 \(f_\psi: O \to [0,1]\)，输入为多模态观测（腕部图像、侧方图像、本体感觉相对于初始帧的残差）。使用均匀进度伪标签 \(p^*_t = t/T\) 和折扣蒙特卡洛回报损失训练。通过检测预测进度下降来定位次优段起点 \(t_a\) 和恢复点 \(t_r\)。
    2. **偏好感知反事实校正**：在干预状态 \(s_{t_h}\) 处，计算反事实优势 \(A_{cf} = \max(Q_{\bar{\phi}}(s_{t_h}, a^\pi_{t_h}) - Q_{\bar{\phi}}(s_{t_h}, a^{human}_{t_h}), 0)\)（Stop-gradient）。对识别的次优段内每个时间步 \(t\)，用位置指数权重 \(\omega(t)=\exp(-\beta \cdot \frac{t_r - t}{t_r - t_a})\) 将 \(A_{cf}\) 从标准Bellman目标中减去，得到校正目标 \(\hat{y}^{corr}_t = \hat{y}^{std}_t - \omega(t) \cdot A_{cf}\)。评论家以此目标进行TD学习。
    3. **偏好辅助策略优化**：在干预状态下，将策略的squashed均值 \(\tanh(\mu_\theta(s))\) 向人类纠正动作拉近，远离策略动作（预防数值奇异性）。损失函数为 \(L_{pref} = \mathbb{E}[ \lVert \tanh(\mu_\theta(s)) - a^{human} \rVert^2 + \frac{1}{\lVert \tanh(\mu_\theta(s)) - a^\pi \rVert^2 + \epsilon} ]\)，与标准RLPD actor损失加权组合。
- **整体流程**：在HIL-RL训练循环中，每当收集到干预轨迹，即用进度模型定位次优段，构造偏好对，进行评论家与演员的双侧校准，与RLPD标准更新结合。

## 3. 实验设计：数据集/场景、benchmark、对比方法

- **真实机器人任务**：共5个具有挑战性的真实操作任务：Press（按压）、Insertion（插入）、Pick（抓取）、Pick & Place（抓放）、Assembly（装配）。每个任务采集20条演示轨迹用于进度模型训练和RL早期稳定。
- **硬件平台**：Galaxea A1X 6-DoF机械臂，配备腕部和侧方摄像机。
- **基线方法**：HIL-SERL（当前最先进的HIL-RL框架），在同一实验设置下对比。
- **评估指标**：最终成功率（SR）、平均人类干预率（IR）、训练时间（分钟）。干预率定义为干预步数与总交互步数之比。
- **额外分析实验**：
    - 进度模型定性可视化（3D末端轨迹标红次优段）。
    - 进度模型定量评估：使用路径效率 \(\eta\) 和方向偏差 \(\Delta\theta\) 两个后验运动学指标，比较检测段与正常段。
    - Q值校正分析：比较HIL-SERL与PACT在学习过程中的目标Q值演化、单轨迹值分布、以及相对于蒙特卡洛回报的归一化偏差。
    - 消融实验：在Insertion任务上，分别移除评论家侧校正（C1）和演员侧偏好损失（C2），记录30分钟时的成功率和干预率。

## 4. 资源与算力

- **文中未明确说明**使用的GPU型号、数量、训练时长等算力信息。仅报告了各任务的运行时间（分钟级别），但未描述计算设备。因此**无法判断具体算力开销**，但方法本身轻量（进度模型为小型MLP+冻结ResNet编码），比HIL-SERL额外增加的计算负担不大。

## 5. 实验数量与充分性

- **实验数量**：覆盖5个真实任务（主要对比），1个消融实验（Insertion任务），进度模型定性定量分析（5个任务各提供示例和指标），Q值校正分析（穿插图示和表格）。整体提供了**10+组实验**（含各任务成功率、干预率、训练时间、偏差统计等）。
- **充分性与公平性**：
    - 实验较为充分：包含多难度任务，有主要对比、消融、内部机制分析。
    - 公平性：所有实验在相同真实机械臂和设置下进行，基线为同领域SOTA方法HIL-SERL，使用相同的人机交互协议。
    - 潜在不足：仅与一个基线对比（HIL-SERL），未与其他HIL-RL变体（如早期DAgger、RLPD+演示）对比；消融仅在Insertion任务上执行，未覆盖全任务。

## 6. 主要结论与发现

- **性能提升**：PACT平均成功率从58.0%提升至82.5%（+24.5%），平均干预率从47.1%降至32.3%（-14.8%），平均训练时间从80.7分钟降至63.0分钟，收敛速度加快1.3倍。
- **最复杂任务Assembly**：成功率从10.0%跃升至62.5%，干预率从56.0%降至47.2%。
- **进度模型有效性**：检测出的次优段在路径效率和方向偏差上均显著劣于正常段，与失败模式（犹豫、错误抓取、路径偏移）一致。
- **Q值校正效果**：PACT有效抑制了次优段上的Q值高估，目标Q值演化更保守，单轨迹值低于蒙特卡洛参考，归一化偏差从正转负，而HIL-SERL始终正偏。
- **消融贡献**：移除评论家侧校正（C1）导致成功率大幅下降（60% vs 90%），移除演员侧偏好损失（C2）导致干预率上升（45.0% vs 32.5%），两者互补。

## 7. 优点

1. **零成本偏好信号**：人类干预本身提供了自然偏好对（纠正动作 > 策略动作），无需额外标注或奖励模型。
2. **轻量可插拔**：PACT作为HIL-RL训练框架的附加模块，可与任意基于RLPD的HIL方法集成，推理时无负担。
3. **自监督进度模型**：仅需少量演示数据（20条），无需人工标注进度，采用伪标签和MC回报训练，简单有效。
4. **双向校正**：同时修正评论家（压制Q值高估）和演员（直接对齐人类纠正），互补提升。
5. **物理意义明确**：反事实优势反映了偏好违反程度，位置权重考虑了因果置信度，设计合理。
6. **真实机器人验证**：全部实验在真实物理系统上完成，结果可信度高。

## 8. 不足与局限

1. **进度模型局限性**：依赖自监督伪标签（假设单调递增），对非单调、重复执行的轨迹可能定位不准确；且校正依赖于定位结果，定位误差会向下游传播。
2. **校正粒度粗**：反事实优势提供方向性惩罚而非精确每步校正，适合抑制系统性高估，但不一定能完全消除局部值偏差。
3. **基线对比单一**：仅与HIL-SERL对比，未考虑其他HIL-RL方法（如带奖励塑形、逆强化学习等）或非HIL方法（如纯模仿学习+RL微调）。
4. **消融范围有限**：消融仅在Insertion任务进行，未在其他4个任务上验证各组件的泛化能力。
5. **缺乏分析对操作员疲劳的影响**：虽然干预率下降，但未测量操作员主观负荷或长期一致性。
6. **未讨论对高维/长序任务的可扩展性**：任务均为短时程操作（<150步），进度模型和校正方法在更长时序任务中可能失效。
7. **计算资源未报告**：未给出训练所需的GPU型号、显存、训练步数等，不利于复现评估。

（完）
