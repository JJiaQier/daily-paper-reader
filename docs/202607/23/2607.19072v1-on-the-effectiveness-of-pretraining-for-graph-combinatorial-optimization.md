---
title: On the Effectiveness of Pretraining for Graph Combinatorial Optimization
title_zh: 预训练在图组合优化中的有效性研究
authors: "David Aguado, Daniel Fuertes, Carlos R. del-Blanco, Fernando Jaureguizar"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.19072v1"
tags: ["query:rl-last-mile"]
score: 8.0
evidence: 针对路由问题的图组合优化自监督预训练
tldr: "图组合优化问题（如旅行商问题）的神经求解器通常从头训练，泛化性不足。本文提出自监督预训练框架，通过旋转与反射的几何增广进行图对比学习，强制模型学习不变结构表示与全局距离分布。在TSP1000上，混合增广策略使路径长度提升6.57%，超越非预训练模型。该工作证明了几何预训练是高效缩放神经求解器至大规模实例的关键归纳偏置。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.19072v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1724, \"height\": 650}, {\"url\": \"assets/figures/arxiv/2607.19072v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1701, \"height\": 1214}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.19072v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1713, \"height\": 212}]"
motivation: 探索自监督预训练在路由类图组合优化问题中的有效性，以提升神经求解器的泛化性和扩展性。
method: 提出几何增广（旋转和反射）的图对比学习预训练框架，学习不变的结构表示与全局相对距离分布。
result: "混合增广策略在TSP1000上实现6.57%的路径长度改进，优于非预训练基线。"
conclusion: 几何预训练为高效缩放神经求解器到大规模实例提供了重要归纳偏置。
---

## 摘要
本文介绍了一种针对图组合优化的自监督预训练框架，专门设计用于解决诸如旅行商问题等路由问题的本质。通过利用带有几何增强（特别是旋转和轴向反射）的图对比学习，模型被迫学习不变的结构表示和全局相对距离分布。结果表明，这种预训练策略在各种问题规模上均优于未预训练的模型。值得注意的是，混合策略（结合旋转和反射）在TSP1000上实现了6.57%的路径长度改进，证明了几何预训练是将神经求解器有效扩展到高维实例的重要归纳偏置。

## Abstract
This paper introduces a self-supervised pretraining framework for graph combinatorial optimization specifically designed to address the nature of routing problems like the Traveling Salesman Problem. By utilizing graph contrastive learning with geometric augmentations (specifically, rotations and axial reflections) the model is forced to learn invariant structural representations and global relative distance distributions. Results demonstrate that this pretraining strategy outperforms non-pretrained models across various problem scales. Notably, the hybrid strategy (combining rotation and reflection) achieved a 6.57% improvement in tour length for TSP1000, proving that geometric pretraining is an important inductive bias for effectively scaling neural solvers to high-dimensional instances.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：神经组合优化（NCO）在求解TSP等NP-hard路由问题上展现出潜力，但现有模型通常从零开始训练，泛化能力有限，尤其当实例规模增大时性能急剧下降。其他领域的自监督预训练方法（如GraphMAE、GCA）虽能提取鲁棒表示，但针对的是属性丰富、拓扑稀疏的图（如分子图、社交网络），无法直接应用于路由问题。路由问题的图是“属性贫乏”的——节点仅有二维坐标，且需要捕捉完全连通图中全局相对距离分布，而非稀疏连接模式。
- **核心问题**：如何为路由类图组合优化问题设计有效的自监督预训练框架，以提升神经求解器在不同规模实例上的泛化性和扩展性。
- **整体含义**：本文证明了几何图对比学习（利用旋转、反射等保距变换）可以作为关键归纳偏置，强制模型学习不变的结构表示，从而显著改善零样本泛化到大规模TSP实例的性能。

## 2. 方法论

### 核心思想
利用图对比学习，将同一个TSP实例经过几何变换（旋转/反射）得到的子图视为正对（等价图），将不同实例视为负对。通过InfoNCE损失，迫使编码器为等价图生成相似的嵌入，为不同实例生成差异化的嵌入，从而学习对距离分布和拓扑结构的不变表示。

### 关键技术细节
- **几何增广**：
  - **旋转** \( f_\theta \)：围绕单位正方形中心 \( c=[0.5,0.5]^T \) 旋转角度 \(\theta \sim U(0,2\pi)\)。变换公式：
    \[
    f_\theta(x_i) = R_\theta(x_i - c) + c, \quad R_\theta = \begin{bmatrix}\cos\theta & -\sin\theta \\ \sin\theta & \cos\theta\end{bmatrix}
    \]
  - **轴向反射** \( f_\phi \)：沿经过中心c、方向角 \(\phi\sim U(0,\pi)\) 的轴线反射。使用Householder矩阵：
    \[
    f_\phi(x_i) = S_\phi(x_i - c) + c, \quad S_\phi = 2vv^T - I, \quad v=[\cos\phi,\sin\phi]^T
    \]
  - **混合变换** \( g(x_i) = f_\theta(f_\phi(x_i)) \)：先反射再旋转。
- **对比学习**：使用InfoNCE损失：
  \[
  \mathcal{L}_{\text{InfoNCE}} = -\log \frac{\exp(q^T k_+ / \tau)}{\sum_{i=0}^K \exp(q^T k_i / \tau)}
  \]
  其中 \(q\) 是原始图嵌入，\(k_+\) 是增广图嵌入（正样本），\(k_i\) 是其他图的嵌入（负样本），\(\tau\) 是温度参数。
- **预训练数据**：TSP实例的节点数 \(N\) 从[20,50]均匀采样，确保模型接触不同规模的拓扑，避免过拟合单一尺寸，促进零样本泛化。
- **训练配置**：预训练50个epoch，每个epoch 128,000个实例，batch size 256，总计 \(6.4 \times 10^6\) 个独特的图几何配置。编码器采用GCN [9]。

## 3. 实验设计

- **数据集/场景**：TSP问题，测试规模包括TSP20、TSP50、TSP100、TSP200、TSP500、TSP1000。预训练仅在20-50节点实例上进行，测试时零样本评估更大规模。
- **基准（Benchmark）**：对比无预训练（None）的基线模型，以及仅使用旋转（Rotation）、仅反射（Reflection）、混合策略（Hybrid）三种预训练变体。所有模型均基于相同的GCN架构，并在相同的下游DRL训练设置下比较。
- **对比方法**：论文未与其他图自监督方法（如GraphMAE、GCA等）进行对比，仅与自身无预训练的基线比较。

## 4. 资源与算力

- 文中**未明确说明**使用的GPU型号、数量、训练时长等具体硬件信息。仅提及预训练数据规模（6.4M实例）和训练轮次（50 epochs），但未提供实际运行时间或硬件配置。

## 5. 实验数量与充分性

- **实验数量**：仅一个定量结果表（表1）和一个定性结果图（图2）。表1覆盖6种实例规模（20/50/100/200/500/1000）和4种预训练配置（None/Rotation/Reflection/Hybrid），共24组数据，每组给出平均路径长度和95%置信区间。
- **消融实验**：包含了不同几何增广策略的消融（旋转、反射、混合），缺少对不同预训练超参数（如角度分布、温度τ、epoch数）的消融，也未对比其他预训练范式（如掩码重建、跨图对比）。
- **实验充分性**：实验规模偏小，仅测试了TSP单一问题，未在VRP、CVRP等更复杂路由问题上验证。定性分析仅给出少量示例。尽管如此，结果趋势清晰：预训练随规模增大提升更显著（从TSP20的0.90%到TSP1000的6.57%），支持了核心假设。整体来说，实验在证明几何预训练有效性上足够，但在泛化性和方法对比上不够充分。

## 6. 主要结论与发现

1. **几何预训练全面优于无预训练**：所有三种预训练变体在全部测试规模上均获得更短的路径长度。
2. **混合策略最鲁棒**：结合旋转和反射的混合增广在最大规模TSP1000上实现6.57%的性能提升，且在大多数规模上为最优。
3. **性能差距随规模增大而扩大**：小规模（TSP20）仅提升0.90%，而大规模（TSP1000）提升6.57%，说明几何预训练提供了关键的归纳偏置，使模型能更好地泛化到复杂结构。
4. **几何对称性是不变表示的有效来源**：旋转和反射保距变换不改变节点间距离，从而保持最优解不变，迫使编码器学习全局相对距离分布和连接模式。

## 7. 优点

- **方法简洁且针对性强**：直接利用路由问题的几何本质（二维坐标、保距对称性）设计增广，比通用图预训练方法更契合任务。
- **零样本泛化能力**：仅在20-50节点上预训练，即可在高达1000节点的实例上获得显著提升，展示出良好的扩展性。
- **训练效率高**：预训练数据规模虽大（6.4M），但增广计算简单（仅矩阵运算），且与下游DRL解耦，可复用。
- **实验设计合理**：提供了置信区间，增强统计可信度；消融了三种增广策略，清晰展示了每种操作的效果。

## 8. 不足与局限

- **实验覆盖不足**：
  - 仅测试了TSP一个问题，未在CVRP、VRPTW等更实际的路由问题上验证方法的通用性。
  - 未与其他主流图自监督预训练方法（如GraphMAE、GCA、SimGRACE等）进行比较，无法确定该方法是否优于现有通用范式。
  - 缺少对预训练超参数的消融（如角度分布范围、温度τ、负样本数量等）。
- **方法局限性**：
  - 几何增广仅适用于节点具有2D坐标且最优解对等距变换不变的问题（如欧几里得TSP），不适用于非欧几何或基于距离矩阵的抽象图。
  - 预训练实例节点规模范围较窄（20-50），虽然实验显示能泛化到1000，但更大规模（如5000+）未测试。
- **资源与可复现性**：未提供代码或详细硬件配置，复现困难。
- **定性分析薄弱**：仅给出少量可视化示例，缺乏对模型学习到的表示的量化分析（如嵌入聚类、t-SNE等）。
- **潜在偏差风险**：预训练时使用的增广是旋转和反射，但未考虑缩放、平移等变换，这些变换对欧几里得TSP同样保距？实际上平移和旋转反射组合才能完全保距，但本文未使用平移（仅旋转+反射）。此外，假设“等价图具有相同最优解”成立，但增广后的图是否一定保留最优解不变？对于欧几里得TSP，任何等距变换（等距同构）确实保持距离矩阵不变，从而保持所有哈密顿圈长度不变。因此理论上成立。

（完）
